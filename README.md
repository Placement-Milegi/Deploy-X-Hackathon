# Deploy-X-Hackathon
This project analyzes student performance data to predict academic outcomes using machine learning. Initially, a regression model is trained to predict marks obtained, which is then transformed into a classification problem to determine whether a student passes or fails based on a defined cutoff.
